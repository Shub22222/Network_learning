Background / Scenario

A small business owner cannot connect to the internet with one of the four PCs in the office. 
All the PCs are configured with static IP addressing using 192.168.1.0 /24 network.  
The PCs should be able to access www.cisco.pka webserver. Use the ipconfig /all command to identify which PC is incorrectly configured.


there was 4 pc's,  pc2 is misconfigurd with IP address, that was 192.168.10.102, so i use commands like
 -  ipconfig /release
but port is not using DHCP

 next command was
 - ipconfig /renew
then pc2 got 192.168.1.102

and able to connect www.cisco.pka
